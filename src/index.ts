/**
 * @prefig/upact-supabase — Supabase Auth adapter for upact.
 *
 * v0.1-draft scaffold. Implementation pending — code lifts from
 * dyad.berlin's `refactor/identity-service` branch.
 *
 * Will export:
 *   - SupabaseUpactAdapter — implements IdentityPort
 *   - EmailChannel — capability-bound email delivery (separate from UserIdentity)
 *
 * License: Apache-2.0
 */

export {};
